services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
    environment:
      - MARKITDOWN_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

  client:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - server
